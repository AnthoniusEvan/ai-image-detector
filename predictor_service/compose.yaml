services:
  app:
    build: .
    container_name: predictor_service
    ports:
      - "8080:8080"
    volumes:
    - .:/app
    command: uvicorn main:app --host 0.0.0.0 --port 8080 --reload
    restart: always
